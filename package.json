{
  "name": "cookiebuddy",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/core.test.mjs tests/popup.integration.test.mjs",
    "check": "node --check src/popup.js && node --check src/content.js && node --check src/details.js && node -e \"for (const file of ['manifest.json','_locales/en/messages.json','_locales/de/messages.json']) JSON.parse(require('fs').readFileSync(file,'utf8'))\""
  }
}
